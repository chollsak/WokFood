'use client'

import { Box } from '@mui/material';
import React from 'react';

const SearchResult: React.FC = () => {
  return (
    <Box>
        <div className='text-white' >Search Result Page</div>
    </Box>
  );
};

export default SearchResult;
